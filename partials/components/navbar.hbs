<nav class="oa-navbar">
  <div class="oa-navbar-inner">
    <a class="oa-navbar-logo" href="{{@site.url}}">
      {{@site.title}}
    </a>

    {{#if @site.navigation}}
      <ul class="oa-navbar-links">
        {{#foreach @site.navigation}}
          <li class="oa-navbar-item {{#if current}}is-active{{/if}}">
            <a href="{{url}}">{{label}}</a>
          </li>
        {{/foreach}}
      </ul>
    {{/if}}

    <a class="oa-navbar-cta" href="/foundations">
      Foundations
    </a>
  </div>
</nav>
